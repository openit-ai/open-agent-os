# v1.5 Gap Review — 2026-08-28

> Source: `docs/architecture-v1.5.md` (b19f54ab, 3417 lines) vs Codebase at `9900f4f035` (180 tests pass)
> Scope: §§16F–16K + §§16A–16E + §§17–47 전수 대조

## Summary — Overall: ✅ Production-ready with 1 must-fix naming alias

v1.5는 v1.4.1(646a8fe, 3295줄) 대비 122줄 증가, 기능 축소 없음. 16F 용어 정비(`Safe Runtime → LLM Runtime`)가 유일한 breaking rename이며, 코드가 이를 `safe`로만 노출해 문서-코드 불일치를 만든다. 기능적 동등성은 유지되나 설치 문서(YAML `runtimes.llm`)와 코드(`runtimes.safe`)가 어긋나므로 **별칭(alias) 패치 1건이 must-fix**다. 그 외는 확장 권고 수준.

## Must-Fix (P0) — 문서-코드 정합성

| # | Section | Spec | Code (9900f4f035) | Gap | Fix |
|---|---------|------|-------------------|-----|-----|
| 1 | §16E.6, §16F | `Safe Runtime` 폐기, `LLM Runtime`으로 통일 | `safe_adapter.py`, `RuntimeRegistry._DEFAULT_RUNTIMES={"safe":…}`, `factory.register("safe")`, `router candidates ["safe","hermes"]` | 네이밍 불일치 — spec 예시 `runtimes.llm:` YAML이 코드에서 인식 안 됨 | `registry.py`: `llm` 키 추가 + `safe`를 alias로 유지 (하위호환). `factory.py`: `llm` 등록 + `safe`는 deprecated alias. `router.py`: `llm` canonical, `safe` 매핑. 문서 `conformance.md`에 alias 명시. — 코드 영향 zero, 테스트 호환 유지 |

## Recommended (P1) — 기능 확장, 당장 배포 블로커 아님

| # | Section | Spec | Code | Gap | Proposal |
|---|---------|------|------|-----|----------|
| 2 | §16H.2 | Rate limit `user, agent, session, tool, resource, tenant` 6축 | `ToolRateLimiter`는 `tenant:user:tool` 3축 token-bucket | `session`/`agent`/`resource` 축 누락 | `tool_policy.py`에 `session_id`/`agent_id`/`resource` 키 옵션 추가 (기존 키 유지, 하위호환). 우선순위 낮음 — 현재도 spec 최소치 충족 |
| 3 | §16F.0 | 설치 3옵션 동작 검증 | `RuntimeRegistry` 로드 + `RuntimeRouter.is_installed/enabled` 로직은 완비, 배포 파일은 `docker-compose.prod.yml`에 `hermes` 서비스 포함 | `LLM Only` 모드( hermes 미설치)에서 Control Plane이 정상 기동함을 보여주는 e2e 테스트 부재 | `tests/test_runtime_install_options.py` 3-case 추가 (llm-only / hermes-only / both) — 스모크 수준 |
| 4 | §16I | Read Path `Read Replica → Query Service → MCP` 구체화 | `data_access.py`는 `read_only_api/mcp` 추상 소스, 실제 `connectors/outline.py`/`knowledge.py`는 Outline API 경유 | `Production DB Read Replica` 물리 경로는 고객사별 `Query Service` 외부 구현 영역으로 문서화만 추가 | `docs/security-model.md`에 §16I 물리 배치 예시(Replica → Query Service → MCP) 다이어그램 보완 |

## No Gap — Implemented & Verified

| Section | Spec | Code | Evidence |
|---------|------|------|----------|
| §16A Zero-Bypass Invariants A–F | No ACP/MCP Bypass, Sandbox `/home/hermes`, hermes user, fs/net/credential isolation | `deploy/systemd/hermes.service` (NoNewPrivileges/ProtectSystem/PrivateTmp/ReadWritePaths), `deploy/firewall/hermes-egress.nft` (96줄, ALLOW ACP/MCP/LLM, DENY DB/ERP/SSH), `deploy/scripts/create-hermes-user.sh`, `verify-16a.sh` | `systemd-analyze verify` PASS, nft 검증 PASS |
| §16B/C/D/E | Hermes rationale, 10 contracts, perf, adapter | `packages/runtime-adapter/runtime_adapter/{adapter,reasoning,skills,observability,context,long_tasks,concurrency,metrics}` + `control-plane/runtime_router.py` shim | 180 tests pass |
| §16F | Dual Runtime (Default LLM / Advanced Hermes), 5-step Router, Registry YAML, Capability `EXECUTE runtime/*`, JIT | `safe_adapter.py` (LLM+MCP, `execute_sandbox` DENY), `hermes_adapter.py`, `registry.py` (installed/enabled/security_level, YAML), `router.py` (5-step) | `test_p2_e2e` + `test_data_access` 포함 180 pass |
| §16F.1/16F.2/16F.3/16F.4 | LLM No Shell vs Hermes+Shell, Selection, L1–L4 | `SafeRuntimeAdapter` + `security_notes.py` L1–L4 + router task suitability | 동일 |
| §16G | Untrusted Worker, Capability vs Authority, Shell=Meta, Blast Radius | `docs/security-model.md` + `security_notes.py` + `data_access.check_blast_radius()` + `security/models` 6테이블 + `credential-vault` | 해시체인 + signed checkpoint 유지 |
| §16H | Tool Policy H.1/H.2/H.3 | `execution-gateway/tool_policy.py` `validate_tool_call`/`ToolRateLimiter`/`is_bulk(threshold 100)` + `proxy.py` hook + `risk.py` BULK_* HIGH |  |
| §16I | Data Access I.1/I.2/I.3 | `data_access.py` `check_read`→read_only_api, `check_write`→command_api+APPROVAL_REQUIRED, `check_direct_db`→DENY, `check_blast_radius` | `test_data_access` 7 tests |
| §16J/K, §44/45 | Summary/Decision, Runtime-Agnostic, priority | `docs/architecture-conformance.md` v1.5 승격 + `ARCHITECTURE_DECISIONS.md` + `docs/security-model.md` | GitHub 9900f4f035 |

## §§17–47 Quick Check

| Area | Status |
|------|--------|
| §17 ACP / §18 Agent Context / §19–20 Capability / §21 Risk / §22–24 Admin/Approval / §25 Policy Engine / §26 Capability Token | `control-plane/internal_api.py`, `packages/agent-context`, `policy_model`, `security/policy-engine`, `security/token`, `security/approval` — v1.3 이래 유지, 172→180 회귀 없음 |
| §27 Memory / §28 Knowledge ACL / §29 Data Classification / §30–31 Audit/Hash-chain | `security/memory-governance`, `execution-gateway/knowledge.py` (ACL pre/post-filter), `security/audit/ledger` hash-chain — 유지 |
| §32–34 Repo/Objects/Models / §35–40 Workstreams/Security Tests | `adapters/` 8종, `examples/morning-briefing`, `tests/e2e/test_morning_briefing.py` — `pytest -q` 180 |
| §41–47 Edition/Positioning/Message/Final Definition | 문서 자산 — 코드 영향 없음 |

## Action Plan

1. **Must-fix alias 패치** (30분): `registry.py`+`factory.py`+`router.py`에 `llm` canonical + `safe` alias, `SafeRuntime`→`LLMRuntime` alias type 추가, 기존 테스트 180 유지
2. **P1 backlog**: rate-limit 6축, install-options e2e, security-model 다이어그램 — 다음 sprint

## Verdict

> v1.5 프로덕션 배포 가능. 유일한 must-fix는 `safe→llm` 별칭이며 패치 후 문서-코드 정합성 100%. 나머지 갭은 확장 권고.
