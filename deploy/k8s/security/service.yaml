apiVersion: v1
kind: Service
metadata:
  name: security
  namespace: open-agent-os
  labels:
    app: security
spec:
  selector:
    app: security
  ports:
    - name: http
      port: 8002
      targetPort: 8002
