apiVersion: v1
kind: Service
metadata:
  name: control-plane
  namespace: open-agent-os
  labels:
    app: control-plane
spec:
  selector:
    app: control-plane
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
