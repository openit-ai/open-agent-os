"""google adapter stub — implement MCP tools/resources for google."""
class GoogleAdapter:
    name = "google"
    async def list_tools(self) -> list[str]:
        return []
    async def list_resources(self) -> list[str]:
        return []
