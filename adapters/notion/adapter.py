"""notion adapter stub — implement MCP tools/resources for notion."""
class NotionAdapter:
    name = "notion"
    async def list_tools(self) -> list[str]:
        return []
    async def list_resources(self) -> list[str]:
        return []
