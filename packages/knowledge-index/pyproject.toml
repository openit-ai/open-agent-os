[project]
name = "open-agent-os-knowledge-index"
version = "0.1.1"
description = "Knowledge Index chunking, embedding boundary, and connector sync orchestration for OAOS"
requires-python = ">=3.11"
dependencies = ["pydantic>=2.7"]

[tool.pytest.ini_options]
asyncio_mode = "auto"

[tool.ruff]
line-length = 100
target-version = "py311"
