"""E2E: Morning briefing flow (Section 36 MVP Demo)."""
import pytest

pytestmark = [pytest.mark.external, pytest.mark.slow]

def test_morning_briefing_mvp():
    # Requires: Mattermost + Outline + Hermes + Google Workspace + Gmail/Calendar/Drive/Tasks
    assert True
